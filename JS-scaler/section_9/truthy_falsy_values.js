// ! Every value having a boolean value in JavaScript

// * falsy
// ? false
// ? 0 (zero)
// ? -0 (minus zero)
// ? on (Biglnt zero)
// ? "", ``, '' (empty string)
// ? null
// ? undefined
// ? NaN

let a = 2;

if(a){
    console.log(`i am truthy`);
}

else{
    console.log('I am falsy');    
}