// ! scope and value assigning to the varibles in the flow of code..
// * using data types with strings

// ! primitive data types:
// ? numbers
// ? strings
// ? null
// ? undefined
// ? boolean

// ! reference data types


let firstPerson = 'adam';
let secondPerson = firstPerson;

firstPerson = 'steve';

console.log('first person: ' + firstPerson);
console.log('second person: ' + secondPerson);

// * with using functions

let firstper = {
    name : 'adam',
    age : 46
}

let secondper = firstper;

firstper.name = 'steve';

console.log(firstPerson);