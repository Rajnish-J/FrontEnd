// ! constant keyword does not allow re-assign, re-declration and works on the block level

const v = 18;

console.log(v);

// ? const v = 17;
// ! re-declaration is not possible

// ? v = 17;
// ? console.log(v);
// !re-assign is not possible

